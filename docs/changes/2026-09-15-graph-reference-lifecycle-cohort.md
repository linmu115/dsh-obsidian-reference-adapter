# 图谱引用生命周期兼容

引用适配器发布 `0.3.4-rc2.13`，显式接受 Annotation Core `0.3.12-rc2.9` 和桥接生命周期包 `0.3.3-rc2.13`。保留既有兼容范围与 Obsidian 引用行为，使新的图谱引用恢复及来源删除包组可以共同安装。

构建通过，现有 6 个测试文件、30 项测试全部通过。开发依赖归档和锁文件保持不变。本次只提交本地；运行副本部署另行记录。
